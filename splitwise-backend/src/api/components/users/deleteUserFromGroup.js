const removeUserFromGroup = (req, res, user_id) => {
    //check balances the person has in the group
    //if there's outstanding balance, then don't remove.
}

module.exports = { "removeUserFromGroup": removeUserFromGroup }